kind: nla_model
role: actor
version: v0.1.x_cheap_path_step_000100
base_model: google/gemma-4-E2B
extraction:
  d_model: 1536
  layer: 23
  injection_scale: sqrt_d_model
  mse_scale: sqrt_d_model
tokens:
  injection_token_id: 249568
  injection_left_neighbor_id: 236813
  injection_right_neighbor_id: 954
av_lora:
  r: 64
  alpha: 128
  target_modules_regex: 'model\.language_model\.layers\.\d+\.(self_attn|mlp)\.(q_proj|k_proj|v_proj|o_proj|gate_proj|up_proj|down_proj)'
training:
  lr: 1.0e-4
  max_steps_configured: 500
  steps_completed_at_this_checkpoint: 100
  loss_at_step_1: 3.7358
  loss_at_step_50: 2.3843
  loss_at_step_100: 2.2389
  micro_batch: 1
  grad_accum: 16
  effective_batch: 16
  max_length: 512
  optimizer: adamw_8bit
  save_interval: 50
training_data:
  path: experiments/v8_nla_local/data/stage3_v0_1_full/av_sft.parquet
  n_rows: 4734
  source: Gemini CLI with persona+audit pipeline (Dr Chen labeler + Dr Otsuka auditor)
  source_families: 9 (Wikipedia, FineWeb-Edu, arXiv, Gemma-4-E2B in-repo deception completions, PKU-SafeRLHF, Anthropic/discrim-eval, Anthropic/persuasion, CAI harmless, Anthropic/llm_global_opinions)
eval_provenance:
  ablation_pending: true
  note: |
    No H5-style ablation has been run against this specific checkpoint yet.
    Per the cheap-path release plan, ablations are scheduled at step_000200
    (mid-training snapshot) and at step_000500 (final), plus a full
    trend-table ablation across all 10 saved checkpoints after training
    completes.
provenance_notes: |
  v0.1.x cheap-path SCALE-UP experiment. Tests whether 6.8x more data +
  8x more SFT steps relative to v0.0.x can break the AR content-blindness
  ceiling established by H13+H14. Same hardware (4 GB GTX 1650 Ti Max-Q),
  same model (google/gemma-4-E2B), same descope choices (NF4+LoRA r=64,
  forward-hook injection, max_length=512).

  This step_000100 checkpoint represents 100 SFT steps × grad_accum 16 ×
  batch 1 = 1,600 row exposures on the 4,734-row corpus = ~0.34 epochs.
  Loss has descended 3.74 -> 2.24 (delta -1.50 over 100 steps).

  Whether the content-sensitivity component of round-trip cos has shifted
  vs the v0.0.x baseline (+0.024 mean delta) is an open empirical
  question pending the H5-style ablation scheduled at step_000200.
