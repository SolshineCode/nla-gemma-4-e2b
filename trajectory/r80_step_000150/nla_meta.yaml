kind: nla_model
role: actor
version: v0.1.y_r80_step_000150
base_model: google/gemma-4-E2B
extraction:
  d_model: 1536
  layer: 23
  injection_scale: sqrt_d_model
  mse_scale: sqrt_d_model
tokens:
  injection_token_id: 249568
  injection_left_neighbor_id: 236813
  injection_right_neighbor_id: 954
av_lora:
  r: 80
  alpha: 160
  trainable_params_approx: 120_000_000
training:
  lr: 1.0e-4
  max_steps_configured: 500
  steps_completed_at_this_checkpoint: 150
  loss_at_step_50: 2.4855
  loss_at_step_100: 2.5038
  loss_at_step_150: 2.2514
  micro_batch: 1
  grad_accum: 16
  effective_batch: 16
  max_length: 384
  optimizer: adamw_8bit
  save_interval: 50
comparison_to_v0_1_x_cheap_path:
  cheap_path_step_150_loss: 2.2504
  v0_1_y_step_150_loss: 2.2514
  delta_at_step_150: +0.001
  honest_reading: |
    At step_150, r=80 has fully closed the +0.27 step_100 gap. Loss is
    now essentially identical to r=64 cheap-path at the same step.
    Strong suggestion that the capacity bump from r=64 to r=80 does NOT
    move the loss floor — both converge to the same 2.20-2.30 plateau
    seen in v0.1.x cheap-path from step 100 onward.

    Caveat: r=80 is only 2.4x params over r=64. The original target r=128
    (3.9x) didn't fit on this 4GB card. Cannot rule out that a larger
    LoRA rank would move the floor — the answer requires the 4090 rental.
provenance_notes: |
  AV capacity bump experiment. Largest LoRA that fits the 4GB GTX 1650
  Ti at this base + max_length config. The mid-trajectory data point
  is now suggesting the 4 GB regime has a floor regardless of LoRA rank
  within the feasible range — the next-best lever is escaping NF4
  quantization on bigger hardware.
